services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "7172:5432"
    environment:
      POSTGRES_USER: ambassador
      POSTGRES_PASSWORD: ambassador
      POSTGRES_DB: ambassador_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
